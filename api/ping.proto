syntax = "proto3";

package api;

option go_package = "api/";

service Pinger {
  rpc Ping (PingRequest) returns (PingResponse);
}

message PingRequest {}

message PingResponse {
  string status = 1;
}